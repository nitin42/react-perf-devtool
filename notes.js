/**

Check if the measure name contains React logo -
  - If it contains the logo, then split again for getting the component name and the phase
    - Join the array that was returned and check whether the string contains a '.' (parsing lifecycle hook). If it contains
    a lifecycle hook name then split and get the component name and the lifecycle hook name
    


*/
